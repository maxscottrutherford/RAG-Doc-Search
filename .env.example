# Application
APP_NAME=RAG Document Search API
DEBUG=false

# PostgreSQL + pgvector (use the same DSN your app reads in app/core/config.py)
DATABASE_URL=postgresql://rag:rag@127.0.0.1:5433/rag_docs

# OpenAI (embeddings + optional chat for RAG answers)
OPENAI_API_KEY=sk-your-key-here
